import logging  # 添加这行导入
from datetime import datetime
from typing import List, Optional
from dataclasses import dataclass
from .storage import SubscriptionStorage
from .models import Subscription  # 从models导入

# 其余代码保持不变...
