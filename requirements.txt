# requirements.txt（更新后）
click>=8.1.7
requests>=2.31.0
python-dateutil>=2.8.2
schedule>=1.2.0
PyYAML>=6.0.1
# 移除 python-daemon，添加 Windows 兼容的库
psutil>=5.9.5  # 用于进程管理（跨平台）
python-daemon 
python-pidfile